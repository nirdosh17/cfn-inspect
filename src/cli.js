export function validate(args) {
  // args contains array of arguments passed from CLI
  // https://docs.npmjs.com/files/package.json#bin

  console.log("CFN Validator CLI Test");
  console.log(args);
}
